@startuml 04-iteration-100
title Iteration 100: What the Agent Actually Sees

start
:Open dotnet-angular-authenticated-full-stack-workflow.html;
:Scan 22 articles;
note right
  Iteration counter is NOT in the file.
  Agent has no idea this is iter 100
  vs iter 1 — same parse either way.
end note
:Find first task with
status != done;
if (that task is BI1
or FI1 or TP3?) then (yes)
  :status = in-progress
  but no sub-progress recorded;
  note right
    GAP: which slice / pass
    am I on? File does not say.
    Agent must re-read git log
    or evaluation notes.
  end note
  :Re-derive progress
  from external sources;
else (no)
  :status drives action
  directly;
endif
:Execute one unit of work;
:Update data-status,
maybe append data-note,
save file;
stop
@enduml
